# /etc/iptables/rules.v6 — управляется Ansible, не редактировать вручную
# Сгенерировано из templates/iptables_v6.j2

*filter
:INPUT DROP [0:0]
:FORWARD DROP [0:0]
:OUTPUT ACCEPT [0:0]

# Loopback
-A INPUT -i lo -j ACCEPT

# Установленные и связанные соединения
-A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT

# ICMPv6 (необходим для нормальной работы IPv6)
-A INPUT -p ipv6-icmp -j ACCEPT

# SSH
-A INPUT -p tcp --dport {{ ssh_port }} -j ACCEPT
{% if ssh_port | int != 22 %}
-A INPUT -p tcp --dport 22 -j ACCEPT
{% endif %}

# Дополнительные TCP-порты
{% for port in allowed_tcp_ports | default([]) %}
-A INPUT -p tcp --dport {{ port }} -j ACCEPT
{% endfor %}

# Дополнительные UDP-порты
{% for port in allowed_udp_ports | default([]) %}
-A INPUT -p udp --dport {{ port }} -j ACCEPT
{% endfor %}

COMMIT
