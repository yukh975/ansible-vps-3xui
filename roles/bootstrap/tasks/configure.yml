---
# =============================================================================
# Фаза configure: 3x-ui, Caddy + ACME, cert-sync, финальная перезагрузка
# Работает на порту {{ ssh_port }} от {{ deploy_user }}
# =============================================================================

# ── 3x-ui ─────────────────────────────────────────────────────────────────────

- name: "[3xui] Проверить — уже установлен?"
  stat:
    path: /usr/local/x-ui/x-ui
  register: _xui_binary
  when: install_3xui | default(true)

- name: "[3xui] Установить 3x-ui"
  shell: |
    set -euo pipefail
    VER="{{ xui_version | default('') }}"
    if [[ -n "$VER" ]]; then
      bash <(curl -Ls https://raw.githubusercontent.com/MHSanaei/3x-ui/main/install.sh) "$VER" <<< "n"
    else
      bash <(curl -Ls https://raw.githubusercontent.com/MHSanaei/3x-ui/main/install.sh) <<< "n"
    fi
  args:
    executable: /bin/bash
  async: 360
  poll: 15
  when:
    - install_3xui | default(true)
    - not _xui_binary.stat.exists

- name: "[3xui] Остановить x-ui перед заменой БД"
  systemd:
    name: x-ui
    state: stopped
  ignore_errors: true
  when: install_3xui | default(true)

- name: "[3xui] Создать /etc/x-ui"
  file:
    path: /etc/x-ui
    state: directory
    owner: root
    group: root
    mode: "0750"
  when: install_3xui | default(true)

- name: "[3xui] Залить x-ui.db"
  copy:
    src: "{{ xui_db_src }}"
    dest: "{{ xui_db_dest }}"
    owner: root
    group: root
    mode: "0600"
    backup: yes
  when: install_3xui | default(true)

- name: "[3xui] Обновить настройки панели в БД"
  shell: |
    sqlite3 {{ xui_db_dest }} "
      UPDATE settings SET value=''              WHERE key='webCertFile';
      UPDATE settings SET value=''              WHERE key='webKeyFile';
      UPDATE settings SET value='127.0.0.1'    WHERE key='webListen';
      INSERT OR REPLACE INTO settings (key, value) VALUES ('webBasePath', '{{ xui_panel_path }}');
    "
  changed_when: true
  when: install_3xui | default(true)

- name: "[3xui] Включить x-ui в автозагрузку"
  systemd:
    name: x-ui
    enabled: yes
    daemon_reload: yes
  when: install_3xui | default(true)

# ── Caddy ─────────────────────────────────────────────────────────────────────

- name: "[caddy] Проверить — уже установлен?"
  stat:
    path: /usr/bin/caddy
  register: _caddy_binary

- name: "[caddy] Установить зависимости"
  apt:
    name: [debian-keyring, debian-archive-keyring, apt-transport-https, curl]
    state: present
  when: not _caddy_binary.stat.exists

- name: "[caddy] Добавить GPG ключ репозитория"
  shell: |
    curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/gpg.key' \
    | gpg --dearmor -o /usr/share/keyrings/caddy-stable-archive-keyring.gpg
  args:
    executable: /bin/bash
    creates: /usr/share/keyrings/caddy-stable-archive-keyring.gpg
  when: not _caddy_binary.stat.exists

- name: "[caddy] Добавить репозиторий"
  shell: |
    curl -1sLf 'https://dl.cloudsmith.io/public/caddy/stable/debian.deb.txt' \
    | tee /etc/apt/sources.list.d/caddy-stable.list
  args:
    executable: /bin/bash
    creates: /etc/apt/sources.list.d/caddy-stable.list
  when: not _caddy_binary.stat.exists

- name: "[caddy] apt update"
  apt:
    update_cache: yes
  when: not _caddy_binary.stat.exists

- name: "[caddy] Установить Caddy"
  apt:
    name: caddy
    state: present

- name: "[caddy] Создать /etc/caddy"
  file:
    path: /etc/caddy
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: "[caddy] Сгенерировать Caddyfile"
  template:
    src: Caddyfile.j2
    dest: /etc/caddy/Caddyfile
    owner: root
    group: root
    mode: "0644"
  notify: restart caddy

- name: "[caddy] Включить Caddy в автозагрузку"
  systemd:
    name: caddy
    enabled: yes
    daemon_reload: yes

# ── Финальная перезагрузка и ожидание готовности ─────────────────────────────

- name: "Финальная перезагрузка"
  reboot:
    msg: "Final reboot after full provisioning"
    reboot_timeout: 300
    connect_timeout: 15
    pre_reboot_delay: 3
    post_reboot_delay: 30

# Ждём пока Caddy получит сертификат через ACME (до 5 минут)
- name: "Ждём сертификат от Caddy (ACME)"
  wait_for:
    path: "/var/lib/caddy/.local/share/caddy/certificates/acme-v02.api.letsencrypt.org-directory/{{ hostname }}/{{ hostname }}.crt"
    timeout: 300

- name: "Запустить x-ui"
  systemd:
    name: x-ui
    state: started
  when: install_3xui | default(true)

- name: "Проверить статус x-ui"
  systemd:
    name: x-ui
  register: _xui_status
  when: install_3xui | default(true)

- name: "Проверить статус caddy"
  systemd:
    name: caddy
  register: _caddy_status

- name: "=== ГОТОВО ==="
  debug:
    msg:
      - "╔══════════════════════════════════════════════════════════╗"
      - "║   Нода готова к работе                                   ║"
      - "╠══════════════════════════════════════════════════════════╣"
      - "║  IP:      {{ ansible_host }}"
      - "║  SSH:     порт {{ ssh_port }}, root закрыт"
      - "║  x-ui:    {{ _xui_status.status.ActiveState | default('не установлен') }}"
      - "║  caddy:   {{ _caddy_status.status.ActiveState }}"
      - "║  TLS:     сертификат получен (Caddy)"
      - "╠══════════════════════════════════════════════════════════╣"
      - "║  Если Ansible больше не нужен — удалите технического     ║"
      - "║  пользователя:                                           ║"
      - "║  userdel -r {{ deploy_user }}"
      - "╚══════════════════════════════════════════════════════════╝"
